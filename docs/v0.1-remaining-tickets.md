# Climb v0.1 — Remaining Tickets (verbatim)

Regenerated 2026-08-15 after the decision-0026 contract/provenance
cutover landed (all rulings resolved). Five open items: the two paid
journeys (WP11e/f), the security review (WP11g), the Gate-2 packet
(WP11h), and the post-approval smoke (rlf). One open Gate-2
sub-decision rides inside WP11h: the release CHANNEL (candidate
staged on 'development'). Execution contracts appended verbatim.
Not included: v0.2 and post-v0.1 tickets and bookkeeping epics.

---

```text
○ techtree-python-ndq.3.5 · WP11e — Clean-machine terminal E2E and failure injection   [● P1 · OPEN]
Owner: Sean Brennan · Type: task
Created: 2026-08-13 · Updated: 2026-08-15

DESCRIPTION

  Spec §9.8, §9.10–9.11. Install-from-zero terminal journey; §9.11 failure-   
  injection matrix (no Docker/uv/credential, dead network mid-run) — every    
  failure lands on a typed error with working repair. Chief gatekeeping (spec 
  §16): no install during register(); no shell/model input into installation; 
  no scores in model-authored output schemas; no auto-retry of the improvement
  turn; no second run without diff+policy display; never call local P1        
  independently reproduced; no upload endpoints. Workspace for plugin tickets:
  techtree-climb/hermes-plugin/ (local checkout of the techtree-hermes repo;  
  git init on first ticket). Plugin runtime never imports Techtree Python; CLI
  JSON envelope is the only boundary.                                         



NOTES

  2026-08-13 chief: heads-up for clean-machine E2E — the python:3.11-slim tag 
  has moved off the pinned digest sha256:90744cff... A clean machine pulling  
  by TAG gets different content than the pinned index; Techtree pulls by      
  digest so the journey should be unaffected, but the E2E should assert the   
  digest-pull path explicitly.                                                
  2026-08-14 decision 0023: execution contract at                             
  docs/release/contracts/wp11e.md — now ALSO blocked by wdc (doctor credential
  truthfulness). Isolated-path clean-machine definition, prime-login journey, 
  full two-comparison live journey with session-close/re-poll, typed failure  
  matrices (no-paid + paid), honest local-artifact scope (public path is WP11-
  postpublish). Budget: estimate first; STOP if remainder insufficient.       
  WP11b finding for the clean-machine journey: left to choose, uv installed   
  the wheel onto Python 3.14.3, OUTSIDE the declared 3.12..<3.14 range, and   
  everything passed silently. The journey must (a) test the supported pinned- 
  3.12 path as primary, (b) record the unsupported-interpreter behavior       
  honestly, (c) consider whether the docs/quickstart must pin the interpreter 
  explicitly.                                                                 



PARENT
  ↑ ○ techtree-python-ndq.3: (EPIC) WP11 — Cross-repository release hardening ● P2

DEPENDS ON
  → ✓ techtree-python-ndq.3.2: WP11b — CLI wheel/package release + fresh-install verification ● P2
  → ✓ techtree-python-e1a: RULING NEEDED: Hermes 0.20.0 cannot install a plugin at a pinned commit — the bootstrap argv's --ref flag does not exist ● P0
  → ✓ techtree-python-ndq.3.3: WP11c — Plugin exact-commit release and bootstrap wrapper ● P2
  → ✓ techtree-python-wdc: Doctor --for-evaluation false-green: exported PRIME_API_KEY passes the check but detached runs still fail ● P1
  → ✓ techtree-python-cxb: RULING NEEDED (Gate-2 structure): embedded placeholder ReleaseCore gates the shipped install flow closed ● P0
  → ✓ techtree-python-ndq.2.5: WP10e — v1-vs-v2 orchestration + terminal/gateway journeys ● P1

BLOCKS
  ← ◐ techtree-python-ndq.3.7: WP11g — Security, privacy, and no-upload review ● P1
  ← ○ techtree-python-ndq.3.6: WP11f — Agent-first Hermes community onboarding E2E ● P1

```

---

```text
○ techtree-python-ndq.3.6 · WP11f — Agent-first Hermes community onboarding E2E   [● P1 · OPEN]
Owner: Sean Brennan · Type: task
Created: 2026-08-13 · Updated: 2026-08-14

DESCRIPTION

  Replaced by decision 0024 (founder ruling: iOS out of v0.1; no phone client;
  no live-phone claim). Certify the reference onboarding path: one pasted     
  prompt to an existing Hermes agent -> reads pinned instructions -> exact    
  install plan -> explicit approval -> plugin install/enable -> one Hermes    
  restart -> plugin installs/verifies the CLI -> doctor -> Hello World, with  
  the twelve acceptance points in the contract (incl. approvals before plugin 
  install / CLI install / paid run, next-step rule on every response, no      
  main/latest/unpinned coordinate anywhere). Execution contract:              
  docs/release/contracts/wp11f.md. Blocked by WP11e.                          



NOTES

  2026-08-14 decision 0023: execution contract at                             
  docs/release/contracts/wp11f.md — blocked on the wp11-gateway-profile       
  FOUNDER decision; declare live vs contract-replay and match release copy to 
  it; 18-step journey requirements; anti-self-approval and duplicate-approval 
  checks.                                                                     



PARENT
  ↑ ○ techtree-python-ndq.3: (EPIC) WP11 — Cross-repository release hardening ● P2

DEPENDS ON
  → ✓ techtree-python-5ig: WP11-gateway-profile — FOUNDER decision: pin the reference gateway ● P1
  → ✓ techtree-python-cxb: RULING NEEDED (Gate-2 structure): embedded placeholder ReleaseCore gates the shipped install flow closed ● P0
  → ○ techtree-python-ndq.3.5: WP11e — Clean-machine terminal E2E and failure injection ● P1

BLOCKS
  ← ○ techtree-python-ndq.3.8: WP11h — Docs, uninstall/upgrade runbooks, founder launch gate ● P1

```

---

```text
◐ techtree-python-ndq.3.7 · WP11g — Security, privacy, and no-upload review   [● P1 · IN_PROGRESS]
Owner: Sean Brennan · Type: task
Created: 2026-08-13 · Started: 2026-08-14 · Updated: 2026-08-14

DESCRIPTION

  Spec §9.12–9.13. Supply-chain review; network capture proving no mutation   
  upload; §15.8 checklist. Chief gatekeeping (spec §16): no install during    
  register(); no shell/model input into installation; no scores in model-     
  authored output schemas; no auto-retry of the improvement turn; no second   
  run without diff+policy display; never call local P1 independently          
  reproduced; no upload endpoints. Workspace for plugin tickets: techtree-    
  climb/hermes-plugin/ (local checkout of the techtree-hermes repo; git init  
  on first ticket). Plugin runtime never imports Techtree Python; CLI JSON    
  envelope is the only boundary.                                              



NOTES

  2026-08-13 chief: add to WP11g review scope — the plugin's envelope-conflict
  detection (services/improvement.py envelope_conflicts /                     
  require_envelope_not_overridden) is a worker-designed deterministic         
  statement-level scan; 0010 requires conflicts to be release-test failures   
  but specifies no method. Review its coverage (conservative, affirmative-    
  instruction-only; could miss paraphrase) before the Skill freeze is treated 
  as final.                                                                   
  2026-08-14 decision 0023: execution contract at                             
  docs/release/contracts/wp11g.md — three-part no-upload methodology (static  
  audit + instrumented method log + destination capture), permission modes on 
  disk, scrubber adversarial cases, verifiers push disabled at both layers,   
  Skill-conflict-scan limitation recorded (no LLM scanner in v0.1).           



PARENT
  ↑ ○ techtree-python-ndq.3: (EPIC) WP11 — Cross-repository release hardening ● P2

DEPENDS ON
  → ○ techtree-python-ndq.3.5: WP11e — Clean-machine terminal E2E and failure injection ● P1

BLOCKS
  ← ○ techtree-python-ndq.3.8: WP11h — Docs, uninstall/upgrade runbooks, founder launch gate ● P1

```

---

```text
○ techtree-python-ndq.3.8 · WP11h — Docs, uninstall/upgrade runbooks, founder launch gate   [● P1 · OPEN]
Owner: Sean Brennan · Type: task
Created: 2026-08-13 · Updated: 2026-08-15

DESCRIPTION

  Spec §9.14–9.18, §15. Upgrade/disable/remove/local-data behavior; published 
  docs; §9.17 introductory-Climb calibration gate; FINAL FOUNDER SIGN-OFF     
  ticket — Sean approves release coordinates, founder Skills, and the §15     
  checklist. Chief gatekeeping (spec §16): no install during register(); no   
  shell/model input into installation; no scores in model-authored output     
  schemas; no auto-retry of the improvement turn; no second run without       
  diff+policy display; never call local P1 independently reproduced; no upload
  endpoints. Workspace for plugin tickets: techtree-climb/hermes-plugin/      
  (local checkout of the techtree-hermes repo; git init on first ticket).     
  Plugin runtime never imports Techtree Python; CLI JSON envelope is the only 
  boundary.                                                                   



NOTES

  CONSOLIDATED FINAL PACKAGING PASS DONE (release_packaging, decision 0022),  
  uncommitted in all worktrees. release-inputs: minimum_host_hermes_version   
  0.19.0 -> 0.20.1, maximum_tested_host_hermes_version 0.0.0-placeholder ->   
  0.20.1 (Hermes Agent v0.20.1 2026.8.13 verified locally to carry plugins    
  install --ref COMMIT_SHA). subject_hermes_version stays 0.19.0 (read from   
  the Campaign, not from inputs). pyproject: Private :: Do Not Upload         
  classifier removed; wheel metadata now declares only the two Python         
  classifiers. ReleaseCore digest c0783963 ->                                 
  73d1be8234424aaab9f281bf66937b21d76cfc5d33b3ff2fd9b9a4dc38a1d33c;           
  placeholder_fields 5 -> 4 (max-tested is now bound), placeholder_release    
  still true. Bytes byte-identical across release/,                           
  src/techtree/resources/release/ and ../techtree-plugin/release-core.json    
  (verified fresh). Ash carries no ReleaseCore and its catalog is unchanged   
  and already byte-identical to the python export, so no re-sync was needed.  
  Plugin CLI envelope fixtures release-info.json/release-verify.json re-      
  recorded from the real CLI. Batteries green: python make check 2811 passed/1
  skipped + generated-check clean, make test-integration 279 passed, plugin   
  make check 797 passed/12 skipped + doctor passed, plugin real-CLI contract 6
  passed (cross-repo release agreement live), ash PGUSER=sean mix check 3     
  doctests/225 tests/0 failures. Wheel rebuilt from a pristine git ls-files   
  export of the working tree, three independent exports -> one digest sha256  
  dfed8ad818e95e356933cf25c66fcd3d8d4d00468e0ac227f7d21a035bdfc85f (584836    
  bytes, 154 members); 86e5908e is superseded. Inspection regenerated: 14/14  
  pass incl. new ReleaseCore bytes present, campaign b9e3f00c packaged and    
  hashing to itself, NO do-not-upload marker anywhere in the wheel, exactly   
  the four designed placeholders, no credentials/absolute paths/git or run    
  data, both console scripts resolve. Fresh isolated install re-run on both   
  interpreters (3.14.3 default, 3.12.13 pinned): 10/10 commands pass, release 
  verify --expected 73d1be82 verified true. Scientific fingerprint            
  regenerated: 24 scientific values RECOMPUTED from the artifacts and every   
  one unchanged (campaign b9e3f00c, climb 93c03b3a, catalog 62714b77, engine  
  874cbae0, membership 56f697fb/36 tasks, taskset package 14d9646d, data      
  policy, validation receipt, validation evidence, both Skill digests, subject
  model/sampling/runtime, gate1 digests); only the six release-coordinate     
  fields moved. CHIEF MUST DO AFTER COMMITTING: (1) rebuild the wheel from the
  python commit and confirm sha dfed8ad8 (build is reproducible; that rebuild 
  is the definitive publishable digest); (2) replace release/plugin-release-  
  candidate.json commit with the new plugin commit and delete commit_note; (3)
  replace release/certified-scientific-fingerprint.json release_source_commit 
  with the new python commit and delete release_source_commit_note; (4) add   
  the new commits to release/post-certification-change-classification.json    
  (all release_packaging or test_only; nothing scientific). SEPARATE TICKET,  
  NOT TOUCHED: ash BootstrapRelease still states minimums.hermes_version      
  0.19.0 (priv/bootstrap/development.json, priv/catalog/bootstrap.json) and   
  the website copy says "Hermes 0.19.0 or newer" — WP11d must raise both to   
  0.20.1 to match the ReleaseCore.                                            
  Packaging pass CLOSED OUT by chief: ReleaseCore 73d1be82 (4 designed        
  placeholders remain: cli_source_commit, cli_version? -> check, release_id,  
  starter URL), publishable wheel dfed8ad8 REPRODUCED from commit 38e242b6    
  (the cli_source_commit for the packet), plugin candidate commit 7f812ca5,   
  all records pinned, classification current (21 commits, 106 changes, 6      
  scientific = the re-certified regeneration). Ash BootstrapRelease + website 
  copy still say Hermes 0.19.0 — WP11d raises both to 0.20.1.                 



PARENT
  ↑ ○ techtree-python-ndq.3: (EPIC) WP11 — Cross-repository release hardening ● P2

DEPENDS ON
  → ◐ techtree-python-ndq.3.7: WP11g — Security, privacy, and no-upload review ● P1
  → ✓ techtree-python-y8s: WP11-claims — product-claim-to-evidence matrix ● P1
  → ✓ techtree-python-iy0: WP11-budget — public Campaign budget-contract audit ● P1
  → ○ techtree-python-ndq.3.6: WP11f — Agent-first Hermes community onboarding E2E ● P1
  → ✓ techtree-python-cxb: RULING NEEDED (Gate-2 structure): embedded placeholder ReleaseCore gates the shipped install flow closed ● P0

BLOCKS
  ← ○ techtree-python-rlf: WP11-postpublish — public-coordinate smoke and rollback check (post-Gate-2 only) ● P2

```

---

```text
○ techtree-python-rlf · WP11-postpublish — public-coordinate smoke and rollback check (post-Gate-2 only)   [● P2 · OPEN]
Owner: Sean Brennan · Type: task
Created: 2026-08-14 · Updated: 2026-08-14

DESCRIPTION

  Decision 0023 item 3.4; closeout directive Phase 7. ACTIVATES ONLY AFTER the
  founder's APPROVE CLIMB V0.1 RELEASE. Verify public wheel hash, public      
  plugin commit, served bootstrap digest, starter object digest, fresh public-
  coordinate install, doctor, read-only website, rollback pointer switch (and 
  switch back). Alters no approved bytes; any public artifact whose bytes     
  differ from the approved digest is a stop-and-report event, never a fix-    
  forward. Execution contract: docs/release/contracts/wp11-postpublish.md.    



DEPENDS ON
  → ○ techtree-python-ndq.3.8: WP11h — Docs, uninstall/upgrade runbooks, founder launch gate ● P1

```


# Appendix — execution contracts (verbatim)

---

# Execution contract — WP11e (ndq.3.5): clean-machine terminal E2E + failure injection

Binding: decisions 0022, 0023; spec wp9-wp11 §9.8, §9.10–9.11.
Blocked by: WP11b, WP11c, wdc (doctor credential truthfulness).

## Purpose
Prove a clean user's complete terminal journey — install through two
verified comparisons — on the exact candidate artifacts, and prove
every likely failure lands on a typed error with a working repair.

## "Clean machine", defined honestly
Fresh HOME, TECHTREE_HOME, HERMES_HOME, UV_TOOL_DIR, UV_TOOL_BIN_DIR;
no globally importable techtree; clean plugin install; separate
managed-engine installation. Record whether Docker images were already
cached. This is fresh homes on the existing machine, not a fresh OS —
say so in the report.

## Pre-Gate-2 vs post-Gate-2
This ticket installs the exact LOCAL candidate wheel and the exact
LOCAL plugin commit. It certifies the artifacts, not the public
distribution path — never claim otherwise. The public-coordinate smoke
is WP11-postpublish, after Gate 2.

## The journey (live, paid)
1. Hermes/plugin readiness. 2. CLI absent/isolated. 3. Explicit CLI
install approval. 4. `prime login`. 5. Docker/engine doctor
(now truthful about credentials, per wdc). 6. Hello World prepare.
7. Review shows: episodes, cost ceiling, same agent/tasks, Skill-only
change, provider disclosure, no-Techtree-upload. 8. Explicit approval.
9. Start → run ID. 10. CLOSE the initiating terminal/session.
11. Poll from a new process. 12. Deterministic result. 13. Verify
local proof. 14. Request one guided revision. 15. Confirm exactly one
model request. 16. Review exact Skill diff and second budget.
17. Explicit approval. 18. v1-vs-v2 run. 19. Verify second proof.
20. Confirm no artifact upload.

Budget: estimate before running; the founder pool remainder (~1.03 of
the 3.00 programme cap at contract time) must cover the estimate, or
STOP and request a new explicit budget authorization. Never silently
substitute fixtures for the live journey.

## Failure-injection matrix — no paid inference
uv absent · docker executable absent · docker daemon stopped · engine
absent · stale engine .installing marker · Prime auth absent · starter
URL unavailable · starter digest mismatch · ReleaseCore mismatch ·
plugin/CLI version mismatch · unwritable techtree home · tampered
proof bundle.

## Failure-injection matrix — paid/provider-path
network fails after run start · provider auth fails · worker process
killed · terminal disconnects · one required episode incomplete · cost
ceiling reached.

For every case: stable typed error code · honest message · working
repair action · no partial valid UpliftReport · no secret leakage.

Note from prior session: python:3.11-slim's tag has moved off the
pinned digest — assert the digest-pull path explicitly.

## Outputs
release/acceptance/terminal-e2e.json · terminal-e2e.md · scrubbed
transcript · run IDs · proof digests · actual cost · failure-matrix
results.

## Stop conditions
Estimate exceeds remaining authorized budget · any failure case
without a typed error + working repair · any upload observed · any
claim that this certifies the public install path.

---

# Execution contract — WP11f (ndq.3.6): agent-first Hermes community onboarding E2E

Binding: decision 0024 (replaces the phone/gateway framing); decision
0023; spec wp9-wp11 §9.9 (channel behavior, bounded output — the
channel-generic requirements survive; the phone-client framing does
not). Blocked by: WP11e (candidate artifacts exist and the terminal
journey passed).

## Purpose
Certify the reference onboarding path: a Hermes community user pastes
ONE prompt to their existing Hermes agent, and everything from reading
the pinned instructions through the Hello World result happens with
explicit approvals at every boundary. No iOS, no named phone client,
no live-phone claim anywhere.

## The path under test
User prompt → Hermes reads the pinned Techtree plugin instructions
(techtree.sh/start and/or the pinned GitHub commit) → shows the exact
install plan → explicit approval → installs/enables the plugin → one
Hermes restart → plugin installs/verifies techtree-python → doctor →
Hello World Climb.

## Acceptance (all twelve, from 0024 §3)
1. A user with Hermes already installed can paste one instruction.
2. Hermes reads the exact pinned GitHub release instructions.
3. It explains prerequisites, commands, cost, provider disclosure,
   and local-data policy.
4. It asks before installing anything.
5. It installs/enables the exact plugin release.
6. It tells the user to restart/reset Hermes once.
7. After restart, the plugin offers the pinned CLI installation.
8. It asks before installing the CLI.
9. It runs release verification and doctor.
10. It starts Hello World only after the paid-run approval.
11. Every CLI/plugin response includes the useful next step.
12. No command uses `main`, `latest`, or an unpinned package
    coordinate.

## Notes
- Pre-Gate-2, the "pinned GitHub release" is the local candidate
  commit served/read locally; the journey must not pretend the public
  repo exists yet. The public-path repeat is WP11-postpublish.
- Paid runs inside the journey follow the same budget rule as WP11e
  (estimate first; STOP on shortfall).
- Channel hygiene from the old contract still applies where output
  flows through a conversation: bounded pull-based status, no ANSI,
  no raw logs, the model can never approve its own action,
  duplicate/replayed approvals never start duplicate runs, session
  loss never kills the worker, a later session recovers by run ID.

## Outputs
release/acceptance/onboarding-e2e.json · onboarding-e2e.md · the
scrubbed conversation transcript · run/proof IDs for any paid legs.

## Stop conditions
Any auto-install without approval · any unpinned coordinate in any
surfaced command · any claim of a certified phone path · budget
shortfall.

---

# Execution contract — WP11g (ndq.3.7): security, privacy, no-upload review

Binding: decisions 0014, 0015, 0023; spec wp9-wp11 §9.12–9.13, §15.8.
Blocked by: WP11e (reviews its captures).

## Purpose
Final release security review with an executable methodology — the ten
SEC tickets are done; this pass proves the assembled artifacts and the
live journeys uphold them.

## Supply chain
wheel SHA-256 · plugin full commit · lockfiles frozen · no unpinned
production dependency · no arbitrary package index · no install-time
script from model input · no auto-update · no shell=True (argv arrays
only) · ReleaseCore equality across repos.

## Local permissions (verify actual modes on disk)
~/.techtree 0700 · plugin state root 0700 · proposal temp root 0700 ·
private key 0600 · logs 0600 · temporary proposed SKILL.md 0600.

## Secrets — recursive scrubber adversarial cases
Bearer token · token in URL userinfo · token in query string · quoted
JSON API key · private key block · nested list/dict · package-manager
stdout/stderr.

## No-upload evidence — three complementary methods, all required
1. STATIC route/client audit: no receipt/proof/Trace upload method
   exists; the ash release surface is GET/HEAD only.
2. INSTRUMENTED application network log: record method, host, route
   for every Techtree-owned HTTP client during the journeys; assert no
   mutation request to techtree.sh.
3. E2E destination capture: expected destinations only (package
   origin, plugin origin, Docker registry, selected model providers,
   read-only techtree.sh); assert no unexpected Techtree artifact
   destination. TLS capture shows destinations without bodies — the
   method log supplies the method evidence.

## Verifiers push
push disabled in compiled config · disabled in resolved config ·
uploader module/path never invoked.

## Plugin Skill conflict scan — record the limitation
The deterministic scan may miss paraphrases. v0.1 does NOT add an
LLM-based semantic scanner. Instead require: exact founder Skill
digest pinning · manual security review checklist · negative tests for
known conflicting instructions · the hardcoded safety envelope stays
authoritative (the founder Skill cannot override one-turn / no-upload
/ no-auto-run). Record paraphrase detection as a limitation scoped to
future untrusted operator Skills.

## Outputs
release/security-review.json · security-review.md ·
network-method-log.json · destination-capture.json · a finding
disposition table. Every accepted limitation names: risk · why
accepted · scope · future ticket.

## Stop conditions
Any mutation request to techtree.sh · any upload path reachable · any
permission mode looser than specified · any scrubber case leaking.

---

# Execution contract — WP11h (ndq.3.8): docs, runbooks, founder launch gate

Binding: decisions 0013 s5, 0022 item 4, 0023; spec wp9-wp11
§9.14–9.18, §15. Blocked by: WP11f, WP11g, WP11-budget, WP11-claims.

## Purpose
Assemble the Gate-2 release approval packet and stop for the founder.

## Gate-2 packet contents (exact values, no placeholders)
approval packet digest · release ID · ReleaseCore bytes+digest ·
BootstrapRelease bytes+digest (the inactive false-valued candidate) ·
CLI source commit · wheel filename+SHA-256 · plugin repository + full
commit · engine digest · catalog digest · Climb digest · Campaign
digest · TasksetLock digest · TasksetValidationReceipt digest ·
DataPolicy digest · starter Skill file AND tree digests ·
skill-improver digest · runtime image index/platform digests ·
model/provider profile · accepted model-revision warning · budget
contract · exact install argv · exact doctor argv · website origin ·
starter asset URL · placeholder_release = false.

Reject any coordinate that is: latest · main · TBD · empty · a short
commit · an unpinned package range · a mutable image tag without
digest · a missing wheel hash · a missing Skill digest ·
placeholder_release omitted. Also reject if rich_output_skill_digest
is present, rich-terminal-output is registered/packaged, or the public
Climb reference is not hello-world-climb@1.

## Post-rehearsal change classification (0022 item 4)
List EVERY commit after certified 1ad6ecf, classified: scientific ·
non_scientific_copy · release_packaging · documentation · test_only.
For every non-scientific class, prove the certified scientific
fingerprint (release/certified-scientific-fingerprint.json) is
unchanged. The wdc doctor fix appears here as non-scientific
onboarding behavior with its battery evidence.

## Documentation truths (every public surface)
no Techtree account required · provider account/credentials may be
required · model calls go to the selected provider · Techtree uploads
no evaluation artifacts · participant-attested local evidence · not
independently reproduced · synthetic toy mechanism demonstration ·
same-benchmark guided iteration, not held-out generalization · guided
revision is single-SKILL.md in v0.1 (0023 §4) · no measured-uplift
claim for the guided revision (Gate-1 packet §7c).

## Attach evidence
all three repo quality gates · generated-file drift checks · fresh
wheel installation · plugin doctor · clean terminal journey ·
agent-first Hermes onboarding journey (0024) · first and
second proof verification · no-upload network assertion · plugin
disable/remove test · CLI uninstall/data-retention test ·
privacy/security review · budget-contract audit ·
claim-to-evidence matrix.

## Post-approval execution plan (recorded in the packet)
1. Publish exact wheel. 2. Verify public wheel hash. 3. Push/tag exact
plugin commit. 4. Activate the exact approved BootstrapRelease
(pointer switch — no rebuild). 5. Verify served bootstrap digest.
6. Public-coordinate install smoke (WP11-postpublish). 7. No paid
scientific rerun unless artifact bytes differ from certified bytes.
8. Launch report with URLs, digests, rollback commands.

## Stop point
STOP at the exact phrase (FOUNDER_APPROVAL_PHRASES.md, "Final Release
Approval"): `APPROVE CLIMB V0.1 RELEASE` + approval_packet_digest +
release_id + release_core_digest + bootstrap_release_digest. Any
changed byte after approval invalidates it and requires a new packet
digest.

---

# Execution contract — WP11-postpublish: public-coordinate smoke + rollback check

Binding: decision 0023; closeout directive Phase 7. ACTIVATES ONLY
AFTER the founder's `APPROVE CLIMB V0.1 RELEASE`. Alters no approved
bytes.

## Purpose
After publication, prove the public path delivers exactly the approved
bytes, and prove rollback works.

## Verify
public wheel hash (downloaded from PyPI) equals the approved SHA-256 ·
public plugin commit (fetched from github.com/regents-ai/
techtree-hermes) equals the approved 40-char commit · served bootstrap
digest equals the approved BootstrapRelease digest · starter object
URL serves bytes matching the approved file digest · a fresh install
from PUBLIC coordinates completes · doctor passes on that install ·
the website is read-only (GET/HEAD only) · the rollback pointer
switch works and is then switched back.

## Explicitly not done
No rebuilds · no new paid scientific evaluation unless a public
artifact's bytes differ from the certified bytes (which is itself a
stop-and-report event) · no changes to any release object.

## Output
The final launch report: all public URLs, digests, install argv,
smoke results, and rollback commands.

## Stop conditions
Any public artifact whose bytes differ from the approved digest —
stop, do not "fix forward", report to the founder.

