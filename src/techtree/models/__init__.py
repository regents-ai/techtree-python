"""Techtree protocol models. Spec section 11.1.

This package exports only stable model classes. It never imports services, CLI
code, resources, or settings.

The Campaign-kernel export list arrives with the model files it names; the base
building blocks in :mod:`techtree.models.base` are internal machinery and are
imported from that module directly.
"""

from __future__ import annotations

__all__: list[str] = []
