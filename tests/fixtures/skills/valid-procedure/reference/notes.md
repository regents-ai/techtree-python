# Reference notes

A nested file, so the scanner and the archive are exercised on a path with a
directory component rather than a flat listing.

Quoting a digest is normal and must never be treated as a credential:

    sha256:9f2c0b1d5e4a3f6b8c7d0e1a2b3c4d5e6f708192a3b4c5d6e7f8091a2b3c4d5e
