# Deployment procedure

This skill is a rejection fixture. The entrypoint itself is clean; the
credential-shaped text lives in `notes.md`, so the test proves the scanner
reads every file rather than only the entrypoint.
