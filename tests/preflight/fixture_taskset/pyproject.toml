[project]
name = "techtree-preflight-taskset"
version = "0.1.0"
description = "Tiny fixture taskset used only by the Techtree Verifiers preflight"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["techtree_preflight_taskset"]
